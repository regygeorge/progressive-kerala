import { Typography } from "@mui/material";

const Education = () => {
  return (
    <div className="p-6">
      <Typography variant="h4">Our Vision on Education</Typography>
      <Typography variant="body1" className="mt-2">
        Our education policy focuses on skill-based learning, digital classrooms, and global standards in education.
      </Typography>
    </div>
  );
};

export default Education;
